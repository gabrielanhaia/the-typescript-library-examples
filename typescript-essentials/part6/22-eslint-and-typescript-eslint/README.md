# Chapter 22 — ESLint + typescript-eslint

The repo's root `eslint.config.js` is the working canonical example. See it for the recommended setup.

## Files

- `eslint.config.example.js` — the strict-type-checked config from the chapter as a reference (drop-in for a new project).

The repo's actual lint command runs against this config: `npm run lint`.
