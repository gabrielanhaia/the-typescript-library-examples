# Chapter 23 — Formatting (Prettier vs Biome)

## Files

- `.prettierrc.json` — minimal Prettier config matching the book's recommendation.
- `biome.json` — Biome 2.x reference config.

The repo root has both files configured for the actual workspace (see `/biome.json` and `/.prettierrc.json`).
